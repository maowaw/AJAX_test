alert("le fichier JS est exécuté!");

document.getElementById('task-list').insertAdjacentHTML("beforeend", "<%= j render 'task', locals: { task: @task, category: @category }%>");
document.getElementById('task-list').reload()